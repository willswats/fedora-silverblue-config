# folder-structure

## Create Directories

Run these commands:

```bash
mkdir ~/Drive
mkdir -p ~/Local/Repositories
mkdir -p ~/Local/Distrobox/main
```

## Bookmark

Add `Repositories` and `Distrobox` as bookmarks.
